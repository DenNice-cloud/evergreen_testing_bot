const handlePhoneInput = require('./handlePhoneInput');
const handleCityInput = require('./handleCityInput');
const handleNameInput = require('./handleNameInput');

module.exports = {
  handlePhoneInput,
  handleCityInput,
  handleNameInput,
};
